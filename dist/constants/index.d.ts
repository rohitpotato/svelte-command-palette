export namespace defaultAppState {
    let isVisible: boolean;
    let textInput: string;
    let activeCommandId: null;
    let selectedCommandId: null;
    let results: never[];
}
export const THEME_CONTEXT: "themeContext";
